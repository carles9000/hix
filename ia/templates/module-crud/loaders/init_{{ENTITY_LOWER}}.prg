/*-----------------------------------------------------------
  File ......: init_{{ENTITY_LOWER}}.prg
  Author.....: {{AUTHOR}}
  Created....: {{DATE}}
  Description: Loader stub for the {{ENTITY}} module. Its only job
               is to pull the {{ENTITY}} model into the same
               compile unit so its functions get registered with
               the HIX runtime when this HRB is loaded.
               The model self-provisions its DBF on first use.
 -----------------------------------------------------------*/

#include '/models/{{ENTITY_LOWER}}.prg'
