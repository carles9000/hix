{
  "name": "HixMw{{MIDDLEWARE_NAME}} allows request with X-Api-Key",
  "request": {
    "method": "GET",
    "path": "{{PROBE_URL}}",
    "headers": { "X-Api-Key": "any-value" }
  },
  "expect": {
    "status": 200,
    "content_type_contains": "json",
    "body_contains": [ "\"ok\":true", "HixMw{{MIDDLEWARE_NAME}}" ]
  }
}
